{
  "name": "project-1-storage-app-v2",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev": "npm run dev --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "lint:fix": "npm run lint:fix --prefix server && npm run lint:fix --prefix client",
    "format": "npm run format --prefix server && npm run format --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "test": "npm run test --prefix server && npm run test --prefix client",
    "analyze": "npm run analyze --prefix server && npm run analyze --prefix client",
    "check": "npm run check --prefix server && npm run check --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6"
  }
}
